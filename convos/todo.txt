STARTING POINT:
    [START WITH THIS, CHECK LAST JOURNAL LOG] duplicate is complete but check
    then REMAINING METHODS [check their generators as well (map, groupBy, deduplicate, sort & rename is done)]
    [DONE] for joins start by replacing new with .createInstance()
    [DONE] then see how to insert JsonModifier.objectProxy() to validate joinCondition function

ENHANCEMENTS:-
[DONE BUT CHECK] deduplicate will have a resolve function which acts same as group by's aggregator. return first deduplicated object which match condition of resolve function.
write generator for new ObjectArray methods

DOC FIX:-

NEW ADDITIONS:-
[DONE BUT CHECK] ObjectArray will have foundational change for object creation -
    [SEE IF REQUIRED] static emptyInstance(...columns) - this will create an array with only 1 object with keys provided as the column names & all values null. problem is an extra completely empty row
    ADDITIONALLY see if ObjectArray needs another data member like schema which can store the column names

[OUTLINE MADE] HTML table like output - Nice display on page for collapsable table
CSV table output as file download link

THINK FOR BUG FIX:-
[MAYBE NEEDED] JsonModifier does not support nested object proxy. Solution - Create a function which is recursively called when object encountered but skip others like arrays.
sort() may face issue due to this. Not now as project runs in "strict mode" & nested objects are rarely present.

REMAINING METHODS:-
[DONE] window function - start by implementing the generator
pivot function - ...
unpivot/melt function - ...
explode - if any column has arrays then normalize them into separate rows
IDEA: (CAN DO WITH GROUPBY CUSTOM AGGREGATOR) see if reverse of explode is ever needed, then how to implement

----------------------------------------------------------------------------------

PLANS FOR WORK:
[DONE] 1. write JsonModifier as separate $project
[DONE] 2. check it rigorously
[DONE] 3. update ObjectArray's object creation process
[CONTINIOUS CHECK] 4. refactor (add feature to) all existing methods in ObjectArray
5. write new of the ObjectArray methods

----------------------------------------------------------------------------------

MY JOURNAL LOG:-
not sure what consistency to maintain for headers of data
XML always has no-white-space data. Unsure about csv & xlsx

csv is fixed by removing white-space removal logic & text qualifier thing already present
xlsx is left as text qualifier thing is done by excel pre-formatter & I can take only data

[CHECK THIS LATER] if pre-formatter is doing nothing, ie all data is quoted then need to handle it in user logic (probably)

cross join implemented
semi join implemented (stand alone)
self join can be implemented by selecting portions of the table, renaming them & doing required join operation
semi-anti join can be done by doing left/right anti-join & then select required columns

groupBy grouping columns stored again thing fixed by using the key itself

deduplicate() now has a JsonModifier to proxy trap array of objects & validate function on them
groupBy() doesn't need JsonModifier as it fetches the data on which I've to work. So, nothing can be set, deleted or incorrectly get.

[CHECK WITH AI] added ways to work for aggregate with non-numerical values.
[CHECK WITH AI] added pre-defined resolve function to deduplicator, ignores null & undefined, works with string, date & numbers.
[DONE CHECK] for aggregator & deduplicator new functions how it handles all null values in mentioned column

----------------------------------------------------------------------------------

This is a temporary log to remember what to do, becuase of travelling :-

TODO: ALL COMMONLY USED WINDOWING FUNCTIONS ARE WRITTEN. CHECK THEM AGAIN IN WindowingGenerator.
REFACTOR: THERE ARE A LOT OF REPEATATIVE CODE IN WindowingGenerator, AGGREGATORS ARE REPEATED IN GroupingGenerator AS WELL.
        LOOK FOR A WAY TO STOP THIS REPEATATION.

THINKING: MAYBE CHECK THE REFACTORS LATER & START WITH PIVOT & MELT FUNCTIONS
